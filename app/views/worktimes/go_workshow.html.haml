.wrapper
  = render "sheard/header"

  .menu
    %h1
      #{@year}年#{@month}月の実績
    - if @go_work.present?
      %h2 出勤
      %table
        %tr
          %th 出勤日
          %th 出勤時間
          %th 退勤時間
          - @go_work.each do |go|
            %tr
              %td
                = link_to edit_worktime_path(go.id), class: "link" do
                  #{go.year}年#{go.month}月#{go.day}日
              %td
                = go.attendance.strftime("%H:%M")
              - if go.retirement?
                %td
                  = go.retirement.strftime("%H:%M")
              - else
                %td
                  未
    = link_to pre_edit_worktimes_path ,class: "btn" do
      前ページに戻る
