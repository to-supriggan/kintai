- if controller_name != 'sessions'
  = link_to "登録しているユーザーでログイン", new_session_path(resource_name), class: "user_log"
  %br/
- if devise_mapping.registerable? && controller_name != 'registrations'
  = link_to "新規アカウントの作成", new_registration_path(resource_name), class: "user_log"
  %br/
